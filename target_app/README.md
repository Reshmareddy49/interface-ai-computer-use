# Local Target Application

A deliberately plain member-servicing console used as the safe stand-in for a bank back-office application.

It has a multi-step search -> detail flow, runtime validation, a not-found business outcome, and a restricted member to support policy/error demonstrations.

No real credentials or PII are used.
